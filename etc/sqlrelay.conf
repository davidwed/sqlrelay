<?xml version="1.0"?>
<instances>

	<instance id="example" socket="/tmp/example.socket">
		<users>
			<user user="scott" password="tiger"/>
		</users>
		<connections>
			<connection string="user=scott;password=tiger;oracle_sid=orcl"/>
		</connections>
	</instance>

</instances>
