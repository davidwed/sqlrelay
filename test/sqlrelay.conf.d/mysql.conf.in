<?xml version="1.0"?>
<instances>

	<instance id="mysqltest" dbase="mysql">
		<listeners>
			<listener port="9000" socket="/tmp/test.socket"/>
		</listeners>
		<connections>
			<connection string="host=mysql;user=testuser;password=testpassword;db=@HOSTNAME@;foundrows=yes"/>
		</connections>
	</instance>

	<instance id="mysql8test" dbase="mysql">
		<listeners>
			<listener port="9000" socket="/tmp/test.socket"/>
		</listeners>
		<connections>
			<connection string="host=mysql8;user=testuser;password=testpassword;db=@HOSTNAME@;foundrows=yes"/>
		</connections>
	</instance>

	<instance id="mysql5test" dbase="mysql">
		<listeners>
			<listener port="9000" socket="/tmp/test.socket"/>
		</listeners>
		<connections>
			<connection string="host=mysql5;user=testuser;password=testpassword;db=@HOSTNAME@;foundrows=yes"/>
		</connections>
	</instance>

	<instance id="mysql3test" dbase="mysql">
		<listeners>
			<listener port="9000" socket="/tmp/test.socket"/>
		</listeners>
		<connections>
			<connection string="host=mysql3;user=testuser;password=testpassword;db=@HOSTNAME@;foundrows=yes"/>
		</connections>
	</instance>

	<instance id="mariadb10test" dbase="mysql">
		<listeners>
			<listener port="9000" socket="/tmp/test.socket"/>
		</listeners>
		<connections>
			<connection string="host=mariadb10;user=testuser;password=testpassword;db=@HOSTNAME@;foundrows=yes"/>
		</connections>
	</instance>

</instances>
