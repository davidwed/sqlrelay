<?xml version="1.0"?>
<instances>

	<instance id="postgresqltest" dbase="postgresql">
		<listeners>
			<listener port="9000" socket="/tmp/test.socket"/>
		</listeners>
		<connections>
			<connection string="host=postgresql;user=testuser;password=testpassword;db=@HOSTNAME@;lastinsertidfunction=testfunc();typemangling=lookup;tablemangling=lookup"/>
		</connections>
	</instance>

	<instance id="postgresql7test" dbase="postgresql">
		<listeners>
			<listener port="9000" socket="/tmp/test.socket"/>
		</listeners>
		<connections>
			<connection string="host=postgresql7;user=testuser;password=testpassword;db=@HOSTNAME@;lastinsertidfunction=testfunc();typemangling=lookup;tablemangling=lookup"/>
		</connections>
	</instance>

	<instance id="postgresql12test" dbase="postgresql">
		<listeners>
			<listener port="9000" socket="/tmp/test.socket"/>
		</listeners>
		<connections>
			<connection string="host=postgresql12;user=testuser;password=testpassword;db=@HOSTNAME@;lastinsertidfunction=testfunc();typemangling=lookup;tablemangling=lookup"/>
		</connections>
	</instance>

	<instance id="postgresql15test" dbase="postgresql">
		<listeners>
			<listener port="9000" socket="/tmp/test.socket"/>
		</listeners>
		<connections>
			<connection string="host=postgresql15;user=testuser;password=testpassword;db=@HOSTNAME@;lastinsertidfunction=testfunc();typemangling=lookup;tablemangling=lookup"/>
		</connections>
	</instance>

</instances>
